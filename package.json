{
  "name": "project",
  "version": "1.0.0",
  "description": "Full stack todo application",
  "main": "server.js",
  "scripts": {
    "build": "node canary.js && tsc",
    "prepare": "node canary.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.11.3"
  }
}